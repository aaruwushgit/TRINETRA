fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
opencv-python>=4.9.0
numpy>=1.24.0
requests>=2.28.0
ultralytics>=8.0.0

# ── Production & Messaging ──────────────────────────────────────
redis>=5.0.0
kafka-python-ng>=2.2.0
pyjwt>=2.8.0
bcrypt>=4.0.0

# ── ANPR ─────────────────────────────────────────────────────────
# Installed separately in the Dockerfile from the vendored services/alpr
# checkout (`pip install services/alpr[ocr]`), so `import alpr` resolves to
# real detection/OCR code rather than a stub.

# ── Database ───────────────────────────────────────────────────
# SQLite only, for now (DATABASE_URL=sqlite:///./dev.db). Postgres is
# provisioned in docker-compose.yml but nothing currently connects to it —
# alembic, psycopg2-binary and geoalchemy2 were removed because none of them
# are imported by any code path. Re-add all three together if/when the
# backend actually switches DATABASE_URL to postgresql://...
