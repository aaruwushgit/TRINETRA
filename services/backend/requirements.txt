fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
opencv-python>=4.9.0
numpy>=1.24.0
requests>=2.28.0
ultralytics>=8.0.0

# ── Production & Messaging ──────────────────────────────────────
redis>=5.0.0
kafka-python-ng>=2.2.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
geoalchemy2>=0.14.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# ── ANPR (install from local repo) ─────────────────────────────
# Run: pip install -e ../Automatic-License-Plate-Recognition
# OR uncomment this if you copy the repo inside this project:
# -e ./vendor/Automatic-License-Plate-Recognition


# ── Database ───────────────────────────────────────────────────
# SQLite is used for dev (no install needed).
# For production PostgreSQL:
#   pip install psycopg2-binary
#   Set DATABASE_URL=postgresql://user:pass@host/dbname in .env
